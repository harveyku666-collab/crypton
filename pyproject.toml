[project]
name = "bitinfo"
version = "0.1.0"
description = "BitInfo - Digital Currency Trading Platform"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
